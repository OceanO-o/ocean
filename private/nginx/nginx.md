正向代理和反向代理区别
    正向代理在客户端，反向代理在服务器端
    正向代理和客户端在另一个lan中
    反向代理和服务器在一个lan中
nginx作用
    反向代理
    负载均衡
        ip hash
        轮询
 supervisor + gunicoron + nginx + flask
 分别配置gunicoron.conf nginx.conf 启动
 supervisor管理进程，进程挂掉可以自动重启
 
 
 